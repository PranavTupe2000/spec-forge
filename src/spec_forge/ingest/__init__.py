# Purpose: owns everything that turns raw input files into source documents and fragments with
# locators — file loaders, vision extraction, alias resolution and caching (L1,
# docs/design/ARCHITECTURE.md). Depends only on core.
