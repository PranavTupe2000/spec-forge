# Purpose: one route module per resource the frontend consumes (projects, runs, evidence, model,
# questions), kept out of api/app.py so the route surface stays legible
# (docs/design/packagedesign.md §2).
