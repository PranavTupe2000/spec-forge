# Purpose: groups the IR Pydantic models — part, port, connection, attribute, state_machine,
# constraint, requirement, provenance, assumption, conflict, open_question, trace, document — as
# one subpackage of core, so the rest of the system imports a single `core.ir` surface
# (docs/design/packagedesign.md §2).
