# Purpose: owns the versioned typed component archetype library — retrieval and the paired
# SysML/Modelica bindings that let extract and emit inject domain knowledge as data rather than
# control flow (L1, docs/design/ARCHITECTURE.md; rule A8).
