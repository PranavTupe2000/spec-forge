# Purpose: owns the LangGraph state machine that sequences ingest -> evidence -> extract ->
# validate -> emit -> toolchain -> repair, plus its event bus, checkpoints and record/replay
# (16_pipeline_orchestration_spec.md).
