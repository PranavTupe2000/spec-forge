# Purpose: the SysML v2 textual emitter — a Jinja emitter.py plus its templates/ — projecting the
# validated IR into structural decomposition, ports, interfaces and connections
# (07_sysml_emission_spec.md).
