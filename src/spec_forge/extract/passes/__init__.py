# Purpose: one schema-constrained extraction pass per concern — structural, topology, attributes,
# requirements, behaviour, constraints, record_detection — assembled by extract/merge.py into one
# IR (docs/design/packagedesign.md §2).
