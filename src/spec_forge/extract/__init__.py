# Purpose: owns the schema-constrained LLM extraction passes and IR assembly/merge — the only
# place a missing value may be inferred with a stated assumption (SF-IN-01); emit/ never infers
# (rule A2). Depends on ingest, evidence and library.
