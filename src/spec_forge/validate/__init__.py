# Purpose: owns the rule catalogue and the four-gate validation ladder, plus the SysML/Modelica
# round-trip consistency check that keeps the two emitted outputs from silently diverging
# (ADR-001; L2, docs/design/ARCHITECTURE.md).
